---
interface Props {
  number: number;
  title: string;
  summary: string;
  bookSlug: string;
  slug: string;
  hasMap?: boolean;
  bookTitle?: string;
}
const { number, title, summary, bookSlug, slug, hasMap = false, bookTitle } = Astro.props;
---
<article class="card chapter-card" data-number={String(number).padStart(2, '0')}>
  <span class="chapter-number">{bookTitle ? `${bookTitle} · Capítulo ${String(number).padStart(2, '0')}` : `Capítulo ${String(number).padStart(2, '0')}`}</span>
  <h3><a href={`/${bookSlug}/${slug}`}>{title}</a></h3>
  <p>{summary}</p>
  <div class="chapter-meta">
    <span class="chapter-meta-pill">Análise</span>
    {hasMap && <span class="chapter-meta-pill is-map">Mapa mental</span>}
  </div>
  <a href={`/${bookSlug}/${slug}`} data-track="capitulo_aberto" data-track-label={title}>Explorar capítulo →</a>
</article>
